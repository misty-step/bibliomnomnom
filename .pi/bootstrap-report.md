# Pi Bootstrap Report

- Domain: bibliomnomnom
- Repo: bibliomnomnom
- Generated: 2026-03-02T18:48:05.591Z
- Package manager: bun
- Stack hints: convex, nextjs, react, tailwindcss, typescript, vitest

## Notes

- Historical reference only. Active operating policy lives in `AGENTS.md` and `.pi/agents/*.md`.
- Fallback plan used because synthesis was unavailable or invalid.

## Repository Context Digest

contextSnippets:
[AGENTS.md]

# AGENTS.md — Operational Playbook

How AI agents work in this repository.

## Codebase Navigation

Every directory contains a `.glance.md` — a pre-computed summary of that directory's purpose, architecture, key files, dependencies, and gotchas. Generated by the `/cartographer` skill.

**Use them before reading files.** When entering an unfamiliar directory:

```text
1. Read <dir>/.glance.md          — architecture, key files, constraints
2. Then read specific files        — only what the task requires
```

This saves tokens and orients you faster than `ls` + reading everything.

`.glance.md` files cover: purpose, architecture patterns, key file roles, subdirectory summaries, important dependencies, and technical gotchas.

When you make significant structural changes to a directory, regenerate its `.glance.md` with `/cartographer`.

## Commit Conventions

Conventional Commits enforced by commitlint (git hook + CI).

**Format:** `type(scope): description` — imperative mood, ≤100 chars

**Types:** `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`

**Scopes:** Module name — `books`, `notes`, `import`, `stt`, `stripe`, `auth`, `observability`, `tracking`, `landing`, `security`, `ci`, `deps`

**Release impact:** `feat` → minor, `fix`/`perf`/`refactor` → patch, others → no release

**Examples:**

```text
feat(import): add Goodreads CSV parser with field mapping
fix(stripe): handle past-due grace period edge case
test(convex): add listening session
```

[CLAUDE.md]

# CLAUDE.md

## What This Is

**bibliomnomnom** — private-first book tracking for voracious readers. Track reading, capture notes via text/audio/photo, import from Goodreads/CSV, share public reader profiles. Built on Next.js 16, React 19, Convex, Clerk, Stripe.

## Essential Commands

```bash
bun run dev              # Next.js + Convex + Stripe listener
bun run dev:no-stripe    # Without Stripe listener
bun run build            # Production build (convex deploy → next build)
bun run build:local      # Next.js build only (CI)
bun run lint             # ESLint
bun run typecheck        # tsc --noEmit
bun run test             # Vitest
bun run test:coverage    # Vitest with coverage
bun run validate         # lint + typecheck + test:coverage + build (full gate)
bun run validate:fast    # lint + typecheck + test (no build)
bun run convex:push      # Sync schema to dev deployment
bun run e2e              # Playwright E2E tests
bun run session-guardrails # Listening session cost/safety checks
```

**bun only** — npm/yarn/pnpm blocked via preinstall hook.

## Architecture

Convex-first with actions pattern. Convex is single source of truth for all data.

| Layer          | Role                                                  | Location              |
| -------------- | ----------------------------------------------------- | --------------------- |
| **Auth**       | Clerk JWT → `requireAuth(ctx)`                        | `convex/auth.ts`      |
| **Data**       | Queries (read), Mutations (write), Actions (external) | `convex/*.ts`         |
| **API Routes** | Webhooks, file upload, listening sessions, OCR        | `app/api/`            |
| **UI**         | React components, Convex hooks for data               | `app/`, `components/` |

[project.md]

# Project: bibliomnomnom

## Vision

Private-first book tracking for voracious readers — capture thoughts on what you read, where you read it, and what it made you think.

**North Star:** The reading companion that serious readers reach for instinctively — capturing every thought in the moment and surfacing insights months later, across every form factor.

**Target User:** Heavy readers (50+ books/year) who currently use Goodreads but feel it's too social/shallow, or who use notes apps but lose the book context.

**Current Focus:** Complete the voice notes → synthesis → notes pipeline (Pillar: voice notes) and make the tracking UX first-class (Pillar: tracking). These two complete the core loop: track what you read, capture what you thought.

**Key Differentiators:**

- Voice notes while reading — speak your thoughts, get structured notes auto-saved
- Private-first — your library is yours, not a social feed
- Reader intelligence — synthesis from voice notes feeds profile/recommendations
- Import from Goodreads/CSV — don't start from scratch

## Domain Glossary

| Term                  | Definition                                                                                 |
| --------------------- | ------------------------------------------------------------------------------------------ |
| **Listening Session** | A recording event tied to a book: audio → transcription → synthesis → notes                |
| **Synthesis**         | LLM-generated structured output (quotes, insights, questions) from a session transcript    |
| **Artifact**          | A synthesis output stored in the `synthesisArtifacts` table                                |
| **STT**               | Speech-to-text. ElevenLabs (primary), Deepgram (fallback), AssemblyAI (candidate provider) |

[ARCHITECTURE.md]

# Architecture

## Overview

bibliomnomnom is a privacy-first book tracking application built with Next.js 16, React 19, and Convex. The architecture follows a "Convex-First with Actions" pattern where Convex is the single source of truth, providing real-time updates, type safety, and clean module boundaries.

**Core Philosophy**: Deep modules with simple interfaces that hide complexity behind clear contracts. Authentication, privacy filtering, auto-dating, and file uploads are abstracted into focused modules that can be understood and modified independently.

## Design Principles

### 1. **Convex as Single Source of Truth**

All data operations flow through Convex for consistency and real-time updates. No client-side state management beyond local UI concerns. Convex provides reactivity automatically—components re-render when data changes.

### 2. **Queries for Reads, Mutations for Writes, Actions for External**

- **Queries**: Pure functions that read database state (cacheable, reactive)
- **Mutations**: Transactional writes to the database (atomic, consistent)
- **Actions**: External API calls or file operations (non-transactional)

### 3. **Row-Level Security in Queries**

Privacy enforced at the query level via ownership checks. Every book/note query filters by `userId`. Users can only access their own data or explicitly public books.

### 4. **Optimistic Updates**

UI updates instantly before…

## Single Highest-Leverage Addition

- Idea: Establish a minimal planner -> worker -> reviewer local workflow loop that compounds repo familiarity through memory-first context reuse.
- Source lane: ambition-pass
- Why now: This creates immediate throughput gains with low maintenance while preserving room for optional advanced overlays.
- 72h validation experiment: Run this bootstrap on two active tasks, then compare plan-to-merge latency and rework churn against the prior baseline.
- Kill criteria: If cycle time or defect/rework metrics worsen by more than 15%, roll back to prior local config and revisit assumptions.

## Lane Evidence

## repo-scout

- model: openai-codex/gpt-5.3-codex
- thinking: xhigh
- status: ok
- elapsed: 11s

(no output)

---

## context-bridge

- model: openrouter/anthropic/claude-sonnet-4.6
- thinking: high
- status: ok
- elapsed: 92s

Now I have a comprehensive picture. Let me compile the analysis:

---

## Existing Context Signals

### Agent Context Files in Repo

| File                          | Agent                   | Quality                                                                                                                                        |
| ----------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `AGENTS.md`                   | Pi (global) + this repo | ★★★★★ — comprehensive operational playbook; `.glance.md` nav, commit conventions, testing guidelines, security boundaries, DoD, issue workflow |
| `CLAUDE.md`                   | Claude Code             | ★★★★★ — excellent: commands, architecture table, tech stack, gotchas, env vars, module boundaries, references                                  |
| `GEMINI.md`                   | Gemini CLI              | ★★ — minimal: only "don't start servers" warning. Near-empty.                                                                                  |
| `.claude/settings.local.json` | Claude Code             | Permission allowlist (~100 entries). Mix of `pnpm` (outdated) and `bun` commands. MCP: Exa enabled.                                            |
| `.mcp.json`                   | MCP config              | Exa search tool configured                                                                                                                     |

### Supporting Artifacts

- **`.glance.md`** at root and throughout dirs — pre-computed architecture summaries (Pi-native convention)
- **`.groom/plan-2026-02-23.md`** — grooming session output with prioritized backlog, risk findings, execution order
- **`.flywheel/state.db`** — flywheel tool state (sqlite)
- **`.pi/state/`** — minimal: only session-handoff JSON (fresh/empty)
- **`ARCHITECTURE.md`** — detailed system architecture, ADRs in `docs/adr/`
- **`CONTRIBUTING.md`** — branch naming, testing requirements, PR checklist, hook troubleshooting
- **`project.md`** — product vision, north star, target user, domain glossary

### Key Conventions Detected

1. **Package manager**: `bun` exclusively (enforced by preinstall hook)
2. **Quality gate chain**: `validate:fast` = lint + typecheck + test; `validate` = + coverage + build
3. **CI pipeline**: cerberus.yml (AI review) + ci.yml + release.yml (semantic-release)
4. **Git hooks via lefthook**: pre-commit (trufflehog, eslint, prettier, tsc), pre-push (env validate, test, build), commit-msg (commitlint)
5. **`.glance.md` system**: Every dir has pre-computed context summaries, regenerated with `/cartographer`
6. \*\*Securit…

---

## docs-research

- model: openrouter/google/gemini-3.1-pro
- thinking: high
- status: ok
- elapsed: 90s

## High-Value References

- **[Convex Test (Vitest Mock Framework)](https://docs.convex.dev/testing/convex-test)**  
  **Why it matters:** Convex strictly recommends using the `convex-test` community package alongside Vitest and `@edge-runtime/vm` for local back-end tests. This isolates backend logic without requiring network calls or pushing schema changes to a live branch, keeping agent validation loops under seconds.
- **[Vitest Dual Environments (`environmentMatchGlobs`)](https://vitest.dev/config/#environmentmatchglobs)**  
  **Why it matters:** Next.js UI (React 19) requires a DOM environment (`jsdom` or `happy-dom`), while Convex backend tests heavily prefer edge computing constraints (`edge-runtime`). This official Vitest feature enables running both test types in a single `bun run test` pass by separating environments per directory (e.g., `__tests__/convex/**` runs in edge).

- **[Next.js App Router CI Build Caching](https://nextjs.org/docs/app/building-your-application/deploying/ci-build-caching)**  
  **Why it matters:** Outlines the specific file paths (`.next/cache`) required for caching Server Components and static payloads. Without preserving this, CI validations requested by agents take 5x longer.

- **[Bun CI/CD & Caching (`oven-sh/setup-bun`)](https://bun.sh/guides/runtime/cicd)**  
  **Why it matters:** Confirms that local and CI caches should target `~/.bun/install/cache`. It completely changes pipeline performance, enabling AI agents to rapidly execute `bun run validate:fast` without incurring network latency on package installations.

## Practical Implications for Agentic Workflow

1. **Offline Validation Loops:** Because tests run locally via `convex-test` mocks in Vitest, an agent does not need to use `convex:dev` or `convex:push` to validate backend logic. It can purely rely on `bun run test` to verify database schemas and mutations autonomously.
2. **Environment Awareness:** The agent must understand directory boundaries: tests in `\_\_t…

---

## workflow-critic

- model: openrouter/anthropic/claude-sonnet-4.6
- thinking: high
- status: ok
- elapsed: 279s

Now I have a comprehensive view. Let me compile the adversarial review.

---

## Critical Failure Modes

### 1. Coverage enforcement has a gaping blind spot: all `convex/*.ts` is excluded

The `vitest.config.ts` coverage `include` scope covers only `lib/import/**`, `lib/listening-sessions/**`, and `lib/stripe-utils.ts`. The comment reads _"convex/_.ts excluded (requires runtime mocking, tested via E2E)"\*, but the E2E tests run against a live Convex deployment with secrets — they don't run on PRs from forks, and the `__tests__/convex/` suite (books, subscriptions, auth, listeningSessions, profiles) **is not counted toward the 75% threshold**. This means `convex/subscriptions.ts` (hasAccess, grace period), `convex/auth.ts` (lazy user creation, de-dupe), and `convex/books.ts` (privacy filtering) can regress without CI failing. The security boundary files are explicitly listed in AGENTS.md as "never touch without approval" — but they have no coverage gate enforcing that.

**Failure scenario:** An agent adds a field to `schema.ts`, touches `subscriptions.ts`, and the grace-period boundary check breaks. Convex tests exist but aren't in scope, CI passes, release ships.

### 2. Pre-push hook runs `test` (no coverage) — threshold only enforced in CI

`lefthook.yml` `pre-push.test` runs `./scripts/bun.sh test` (→ `vitest run`, no `--coverage`). The 75%/perFile threshold is only enforced by CI's `test:coverage`. A developer can push code that drops coverage below threshold, and the local gate won't catch it. They only discover the failure after CI runs — after the branch is already remote.

**Failure scenario:** File added to `lib/import/` with 20% coverage. Pre-push passes. PR opens. CI fails. Developer is confused, scrambles to add tests post-hoc, or squash-amends to obscure the gap.

### 3. `webhookEvents.markProcessed` is a public unauthenticated mutation

`convex/webhookEvents.ts` exports `markProcessed` as a plain `mutation` (not `internalMutation`). Any authenticated _or_…

---

## ambition-pass

- model: openrouter/google/gemini-3-flash-preview
- thinking: high
- status: ok
- elapsed: 79s

## Foundation Workflow Pattern: **The "Doctrine-First" Inverted Loop**

Move from "Feature-First" to "Standard-First" implementation. This pattern forces the agent to define the _quality gate_ before the code, ensuring high-leverage outcomes over simple output volume.

1.  **Alignment Check**: Map the task to one of the repository's three core pillars (Voice Notes, Tracking, Reader Intelligence) defined in `project.md`.
2.  **Contract Creation**: Write (or update) a `scripts/guardrails/` script that defines the success invariant (e.g., "A new book mutation must verify that the user is not exceeding their subscription limit").
3.  **Implementation**: Build to satisfy the guardrail.
4.  **Knowledge Capture (The "Trophy" Step)**: If a pattern was discovered (e.g., a specific way to handle Convex transaction limits), the agent _must_ update the directory's `.glance.md` or the global `AGENTS.md` to codify the learning.

## Prompt + Pipeline Ideas

- **The "Complexity Tax" Auditor (Prompt)**: `Analyze this proposed implementation. If this were to be maintained by a developer with zero context in 6 months, which 20% of the code would be the most confusing? Refactor to remove that complexity now.`
- **The "North Star" Filter (Prompt)**: `Before starting, explain how this change moves the product closer to the 'Private-First/Digital Garden' vision. If it doesn't, propose a 'simpler version' that fulfills the technical requirement without product bloat.`
- **Pipeline: `validate:architecture`**: A simple Bun script that greps all Convex mutations to ensure they contain the `requireAuth(ctx)` call, failing the build if a "raw" mutation is found without an explicit `// PUBLIC_ACCESS` bypass comment.

## Minimal Viable Bootstrap vs. Optional Upgrades

| Feature     | **Minimal Viable Bootstrap** (Keep) | **Optional Upgrades** (Ambition)                                     |
| :---------- | :---------------------------------- | :------------------------------------------------------------------- |
| **Context** | `.glance.md` (Static summaries)     | **Dynamic Cartographer**: Auto-generates `.glance.md` on file chang… |

---

## implementation-critic

- model: openrouter/anthropic/claude-sonnet-4.6
- thinking: xhigh
- status: ok
- elapsed: 130s

Now I have enough signal to deliver a thorough critique. Let me synthesize.

---

## Durability Risks

### 1. `ARCHITECTURE.md` describes a codebase that no longer exists

The doc references only 5 modules (Auth, Books, Notes, External Search, File Upload) and claims the test suite has "54 passing tests" with "88% statement coverage" dated 2025-11-25. `project.md` explicitly calls out listening sessions, synthesis, STT gateways, and a `synthesisArtifacts` table—none of which appear in `ARCHITECTURE.md`. The system diagram is missing the entire voice/synthesis stack. **A doc this stale actively misleads agents who read it first.** In six months, any on-call or new contributor following `ARCHITECTURE.md` will have an incorrect mental model of the data layer, state machines, and API routes.

_Risk: HIGH. Root cause: ARCHITECTURE.md was last meaningfully updated at MVP, not maintained as features shipped._

### 2. `CLAUDE.md` references `docs/CODEBASE_MAP.md` — file does not exist

The References table in CLAUDE.md points to `docs/CODEBASE_MAP.md` as "Full architecture map." That file is absent. Agents and engineers following the reference land on a 404. The cross-reference in `.glance.md` also points there.

_Risk: MEDIUM-HIGH. Dead links rot trust in all docs._

### 3. Coverage scope is dangerously narrow

`vitest.config.ts` includes only `lib/import/**`, `lib/listening-sessions/**`, and `lib/stripe-utils.ts`. The 75% threshold enforced in CI applies only to this subset. `convex/*.ts` (auth, books, notes, listening sessions — the entire security and business logic layer) is explicitly excluded with a note: "requires runtime mocking, tested via E2E." But E2E tests run against a live Convex deployment with real secrets — meaning there is **zero automated coverage of Convex backend logic in CI on PRs without secrets** (Dependabot, forks). The listening session state machine, identified in `project.md` as "the core invariant," has no unit-level test coverage.

\*Risk: HIGH.…

## Quality Gate Scorecard

- Gate pass: yes
- Ambition score: 85/100 (pass)
  - novelty: 5/5
  - feasibility: 4/5
  - evidence: 5/5
  - rollbackability: 3/5
- Consensus score: 96/100 (pass)
